import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    ul: {
        justifyContent: 'space-between',
        marginTop: '20px',
        padding:'10px 0px',
    }
}))